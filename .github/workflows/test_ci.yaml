# .github/workflows/tests.yml

name: Go Unit Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
<<<<<<< HEAD
          go-version: '^1.20'
=======
          go-version: 1.20
>>>>>>> Fix ci

      - name: Run tests
        run: go test ./...
